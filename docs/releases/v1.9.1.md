## 更新内容

- 修复带翻译歌词的长英文句：主歌词会优先适配为两行，避免末尾单词被挤到第三行。
- 修复普通逐行 LRC 的伪逐字显示：长句换行窗口会依据实际渲染宽度和高亮位置切换，不会在进度中段提前跳到下一行。
- 修复短歌词被误匹配为同前缀长歌词的问题；例如 `No, no` 不会再显示为 `No, no body, no crime` 及其翻译。

## 验证

- 已执行 `testDebugUnitTest`。
